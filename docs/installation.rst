============
Installation
============

At the command line::

    $ easy_install acdh-django-geonames

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv acdh-django-geonames
    $ pip install acdh-django-geonames
